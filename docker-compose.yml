services:
  qr-generator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: qr-generator
    ports:
      - "3111:80" 
    restart: unless-stopped
    environment:
      - NODE_ENV=production

